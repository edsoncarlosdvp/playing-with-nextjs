import { change_name, nameReducer } from './'

describe('nameReducer', () => {
    describe('change_name', () => {
        it('should add an item to the list when the list is empty', () => { }
        )
    })
})